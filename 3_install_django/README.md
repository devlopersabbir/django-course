### Install DJango

1. Django is installed using pip, with this command:
   - Windows
     ```bash
     py -m pip install Django
     ```
   - Unix/MacOS:
     ```bash
     python -m pip install Django
     ```
2.
